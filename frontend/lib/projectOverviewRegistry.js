export function composeProjectRegistryItems(registryItems) {
  // Skill-First UI 宪法：旧模块入口在 UI 上不可达。
  // 项目总览只作为“容器 + 全局统计 + 等待 Skill 启动”的空白底座，不再展示任何旧模块卡片。
  void registryItems;
  return /** @type {any[]} */ ([]);
}
